/*
 * Terminal.cpp
 *
 *  Created on: Apr 19, 2018
 *      Author: aboelhamd
 */

#include "Terminal.h"

Terminal::Terminal(string name) {
    this->name = name;
    terminal = true;
}

bool Terminal::operator<(const Terminal terminal) const {
//	return name.compare(terminal.getName());
//	string s1 = name;
//	const string s2;
//	basic_string s3 = terminal.getName();
//	= terminal.getName();

// need check
    return true;
}